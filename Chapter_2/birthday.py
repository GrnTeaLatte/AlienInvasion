#strings and integers together
#When you use integers within strings
#like this, you need to specify explicitly that you want 
#Python to use the integer as a string of characters. 
age = 23
message = "Happy " + str(age) + "rd Birthday!"
print(message)

